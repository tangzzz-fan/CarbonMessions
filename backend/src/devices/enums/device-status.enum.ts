export enum DeviceStatus {
    ACTIVE = 'active',         // 运行中
    INACTIVE = 'inactive',     // 停用
    MAINTENANCE = 'maintenance', // 维护中
    FAULT = 'fault',           // 故障
    STANDBY = 'standby',       // 待机
    OFFLINE = 'offline'        // 离线
}